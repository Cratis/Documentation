# Test Storybook

This is a sample Storybook project used to demonstrate the Storybook integration feature in the Cratis documentation site.

## Building

```bash
npm install
npm run build-storybook
```

The built storybook will be output to `storybook-static/`.
