# Cratis documentation overhaul — handover

> Read this first if you're continuing the Cratis docs overhaul in a fresh session.
> It captures the goal, the current state, how the site is built, how to run and verify it,
> the writing bar, what's done, what's next, and the gotchas that will bite you if you don't know them.

## 0. Fresh-start brief (2026-05-31 — read this first; supersedes any stale numbers below)

### ⏭️ NEXT SESSION — START HERE (user priorities, set 2026-05-31)

The next session is a **visual + accuracy QA pass**. The user gave these explicitly — do them roughly in order. (Everything else in this handover is still-valid context, but these six are the active work.)

> **✅ Progress 2026-05-31 → 06-01 (visual polish session — COMMITTED, NOT pushed; still 678 pages · 0 · 0):**
> Addressed the user's two concrete complaints + an aspire-style depth pass.
> - **⚠️ DEV-SERVER DEGRADATION (operational gotcha — this is what made tables look broken "throughout the docs").** Running `npm run check`/`npm run build` **while `npm run dev` is live** re-runs `sync-content.mjs` (the `prebuild` hook), which rewrites `src/generated/topics.json` + the synced content dirs the dev server is watching → the dev server falls into a bad state: every page 500s (`AstroUserError` / 80-byte body) or throws `RenderError: slug "chronicle" … does not exist`, and GFM tables silently stop rendering. **A long dev session HMR-rots into the same state.** Fix: **restart `npm run dev`** (kill `lsof -ti tcp:4321`, re-run). **Don't run the gate against the same tree while dev is up** — or expect to restart after. This is separate from the real MDX-table build bug (now fixed). Re-verify a clean dev server before trusting any "X doesn't render" screenshot.
> - **POLISH CONTINUATION (2026-06-01) — committed `ecc79dc`, `c6af0d8`.** Brand-tinted **inline code** chips (aspire pattern: surface mixed from `currentColor`, accent text, hairline border — scoped `:not(pre)` so code blocks are untouched), a **tinted table header**, brand **`::selection`**, and **card depth** (SimpleCard + Starlight `.card`/`.sl-link-card` get a top-right accent-glow gradient + lifted surface + inset highlight + a stronger hover lift). Verified light+dark. All in `cratis.css` + `SimpleCard.astro`.
> - **Diagrams (was "ugly black/white boxes") — DONE.** astro-mermaid rendered with the stock `default`/`dark` themes (drab). Now brand-themed via a CSS layer over the rendered SVG (`cratis.css` "Mermaid diagrams" section) — gradient-glow frame (adopted from aspire's `mermaid.css`), accent-bordered rounded node cards, recessed subgraph panels, brand-blue edges; covers flowchart/cluster/edge/**sequence**/**state**; flips with light/dark. Mermaid's own SVG rules use `#id` selectors but **no `!important`**, so class selectors with `!important` win. Gave the integration `mermaidConfig.fontFamily: Inter` (`astro.config.mjs`) so it **measures boxes with the render font** — otherwise labels clip. **Gotcha:** never bump label `font-weight` via CSS post-render — it widens text past the measured box and clips (cost me a round). Verified light+dark on flowchart, nested-cluster, and sequence diagrams. Commits: Documentation `d87f971` + `8e3cde3`.
> - **Code blocks "indented / leading spaces" — DONE.** Not CSS: **7 code blocks** had a spurious *common* leading indent (snippets lifted from a method body, kept 8/4-space indent). Scanned every product's `Documentation/**` (only 7 total) and dedented to col 0, preserving internal structure. Chronicle `0df1c407e` (get-started common/mongodb/aspnetcore), Fundamentals `521ff42` (csharp/concepts).
> - **Hero + code-block depth (aspire gap) — DONE.** Hero: accent glow behind headline + logo, subtle gradient headline, logo drop-shadow. Code blocks: soft drop shadow (`figure.frame`). All in `cratis.css`, commit `8e3cde3`.
> - **✅ TASK 3 — REINTERPRETED + FIXED. It was never the diagram — it was GFM TABLES.** The user clarified: the front-door comparison **table** rendered as raw `|` pipe text, and "similar throughout the docs." Root cause: **tables silently dropped on EVERY `.mdx` page** (front door, why-cratis, adopting-cratis, …) while plain `.md` was fine. Mechanism: astro-mermaid injects plugins via the **deprecated `markdown.remarkPlugins`** path (build prints the deprecation), which leaves `@astrojs/mdx`'s own `gfm` flag falsy (`getRemarkPlugins` only pushes `remarkGfm` `if (mdxOptions.gfm)`), so MDX never runs remark-gfm. **Fix:** added `markdown: { remarkPlugins: [remarkGfm] }` to `astro.config.mjs` — MDX re-applies the inherited plugin set via `extendMarkdownConfig`, restoring tables. Verified in a fresh **build** (the bug only shows in build / a freshly-restarted dev — a long-running dev server also degrades to 500s + dropped tables, which is how it first looked site-wide). Commit `0ad7cbc`. **Diagnostic tip:** `grep -c '<table' dist/<page>/index.html` after `npm run build`; `.md` ok, `.mdx` was 0. **Watch for:** the same deprecated-`markdown.remarkPlugins` path could bite other MDX-only markdown features (smartypants, autolinks) — sanity-check if something else "doesn't render" on `.mdx`.
> - **✅ TASK 2 (nav flicker / "renders then jumps") — font reflow FIXED; Mermaid CLS is the residual.** Measured CLS with a CDP probe (`/tmp/clsprobe.mjs` — injects a buffered `layout-shift` PerformanceObserver via `Page.addScriptToEvaluateOnNewDocument`, reports total + shifting elements). Findings: (a) **font-swap reflow** was the universal cause — brand fonts load `font-display: swap`, so every page first-painted in a system font then reflowed. **Fixed** with metric-matched fallback `@font-face`s (next/font technique: `size-adjust`+ascent/descent overrides on `local('Arial')`/`local('Menlo')`) + `--sl-font: 'Inter Variable','Inter Fallback'`. Text-heavy pages dropped ~0.12 → ~0.005. Commit `4df5fe4`. (b) **Residual CLS is client-side Mermaid** — astro-mermaid reserves `min-height:200px` then grows to the rendered height; measured heights vary **149–972px**, so NO fixed reservation fixes it. Proper fix = **build-time Mermaid rendering** (swap astro-mermaid for a build-time renderer; needs a browser/deps — a real follow-up). Most non-diagram pages are now **0.02–0.05 (good)**; diagram-heavy pages (get-started, event-source) sit ~0.11. (c) Minor: a couple pages have interactive-component hydration CLS (a PrimeReact dropdown demo). **No View Transitions are enabled** and `scrollbar-gutter:stable` is already set — neither was the cause.
> - **✅ TASK 2 follow-up (2026-06-01) — the "grows-larger-then-pops" twitch on EVERY page — FIXED with font PRELOAD.** The user re-reported a universal "everything grows a bit for 0.1s then normalizes" on load/refresh/nav. Diagnosed with timeline CDP probes (`/tmp/twitch2.mjs` samples `documentElement` + hero/code/mermaid heights every 16ms; `/tmp/eldiff2.mjs` diffs every element's height at the tall frame vs settled). Root cause: the metric-matched **fallback still wraps text differently than Inter** (size-adjust fixes vertical metrics, not per-glyph widths), so cold loads paint in the fallback, then **reflow/re-wrap** when the web font swaps in — the hero H1 visibly shrank 636→601px. In dev every refresh is effectively uncached, so it fired constantly. **Fix:** a Starlight `<Head>` override (`web/src/components/Head.astro`, registered via `starlight.components.Head`) that **preloads** the latin/latin-ext Inter + JetBrains Mono woff2 (imported with `?url` → hashed build asset) so the brand font is ready at first paint and the swap is invisible. Commit `b9f2790`. **⚠️ Preload ALONE was NOT enough** — the user still saw the twitch, because @fontsource ships `font-display: swap`, which still swaps (and reflows) whenever the font arrives after first paint; preload only speeds the fetch. **The real fix (commit `4081717`): declare the faces ourselves with `font-display: optional`** — `Head.astro` now emits the Inter + JetBrains Mono `@font-face`s (latin + latin-ext, `?url` src, full unicode-ranges pulled from @fontsource) with `font-display: optional`, and the `@fontsource/*/index.css` imports were removed from `astro.config` customCss (so no `swap` duplicate). `optional` = the browser uses the preloaded font if ready in a short block window, else keeps the metric-matched fallback for that load — **never a mid-page swap**. Verified via `document.fonts.check(...)` that Inter/JetBrains Mono still load+apply (cold and warm) and render correctly. **Verified:** no-diagram pages (glossary, datapage) are now **shift-free on cold load**, hero stable from frame 1. **The remaining diagram-page twitch is inherent to client-side Mermaid** — `mermaid.render()` injects a temporary measurement node into the DOM (grow) then removes it (shrink); only **build-time Mermaid rendering** removes it. So: font/text twitch = DONE (preload); Mermaid settle = the same documented build-time-rendering follow-up. (Keep the metric fallbacks too — they help the brief first-uncached-paint and other CLS.)
> - **Tasks 4 & 5 DEFERRED by the user (2026-06-01):** "record the other tasks for later." Task 4 (comparison rewrite vs current Marten/Wolverine/Kurrent — verify against `~/src/repos/marten`+`wolverine`), Task 5 (de-Chronicle the Arc tutorial — **needs user confirm on the exact split** first), Tasks 6/7 (awesome-docs tooling: Doc Detective example-testing, Alex/case-police; `.ai` codification + symlink wiring). The user's standing priority order this session was: fix tables (3) → font/CLS (2) → more polish (1), then defer 4–7.
> - **Tooling:** `shot-scraper`/`uvx` aren't installed; built a reliable **headless-Chrome CDP driver** at `/tmp/cdpshot.mjs` (Node 23 global `WebSocket`) that emulates `prefers-color-scheme` so you can capture **light AND dark** (plain `--screenshot` only gives the OS default = dark here). Usage: `node /tmp/cdpshot.mjs <url> <out.png> <light|dark> [width]`. Crop with the `sharp` already in node_modules. **Run cdpshot/clsprobe serially — parallel runs collide on the debug port.**

1. **Screenshot every doc page and evaluate how it looks.** Dev server: `cd Documentation/web && npm run dev` → http://localhost:4321. Capture each page headless and assess. **Prefer `shot-scraper`** (from awesome-docs — `uvx shot-scraper http://localhost:4321/<slug>/ -o out.png --wait 1500`; wraps headless Chromium) over raw flags; fallback `google-chrome --headless --no-gpu --screenshot=out.png --window-size=1440,900 <url>`. Build the page list from `src/generated/topics.json` + the site-level slugs. **THE BAR IS aspire.dev** — the user's blunt verdict: *our site does NOT look nearly as cool as aspire.dev.* Study `~/src/repos/aspire.dev/src/frontend` and do a real **visual-polish pass** to close the gap (hero, spacing, cards, code-block styling, motion, density) — not cosmetic tweaks.
2. **BUG — sidebar/page flicker + jump on navigation.** Clicking a page/category/topic renders then *jumps* (layout shift). Very annoying to the user. Investigate: Astro/Starlight View-Transitions flash, the `starlight-sidebar-topics` rail recomputing per page, font-load FOUC, or Mermaid re-render CLS. Target: **zero layout shift on nav.**
3. **BUG — front-door diagram doesn't render.** On the splash front door (`web/src/content/docs/index.mdx`) a Mermaid diagram fails to render (user placed it near "Coming from another stack?"; the page's only mermaid is the "The whole loop, in one place" block). **Strong hypothesis:** `astro-mermaid` doesn't run on `template: splash` pages. Verify + fix.
4. **Comparison / "Coming from X" pages are INACCURATE — rewrite against how the tools work TODAY.** `web/src/content/docs/comparisons/{marten,wolverine,kurrent}.md` (+ the CRUD/MediatR bridges). Verify every claim against the **local repos** the user has — `~/src/repos/marten`, `~/src/repos/wolverine` (read their *current* docs/source) — and current Kurrent/EventStoreDB. The user says they're "not very accurate." Fix the factual drift.
5. **Remove Chronicle leakage from the Arc PRODUCT.** The user is firm: Arc product docs must be **isolated from Chronicle**, but the **Arc tutorial still uses command→event→projection (Chronicle)**. ⚠️ This **SUPERSEDES** the earlier "additive — keep Chronicle as Arc's flagship" decision. **New direction:** make the canonical **Arc tutorial Chronicle-free** (Arc over MongoDB/EF — reuse the `arc-without-event-sourcing` shape) and move the Arc+Chronicle *combination* to the **Cratis Stack tour** / a separate combined capstone. `grep -ri chronicle Arc/Documentation` to find leakage. **But do NOT delete the legitimate `backend/chronicle/` integration category** — that's Arc's *optional* Chronicle integration ("the line" the user affirmed earlier). The fix is making the **tutorial + getting-started Chronicle-free by default** (and clearly framing the combination as opt-in), not removing the integration reference. **Confirm the exact split with the user before rewriting the tutorial.**
6. **awesome-docs — now consulted** (`https://raw.githubusercontent.com/testthedocs/awesome-docs/refs/heads/main/README.md`). Apply: **`shot-scraper`** (task 1); **Doc Detective / Squidler** = *automated example testing* (actually RUN snippets) — the systematic fix for the snippet-bug problem we've hand-audited, **evaluate adopting it**; **Alex / case-police / Harper** (inclusive-language + case linters to extend the wired Vale/markdownlint/lychee QA stack); **HtmlTest / Linkspector** (built-HTML/link QA). Reading: jvns "Write good examples by starting with real code" (validates snippet-from-source); **Semantic Line Breaks** (sembr.org).
7. **Continue codifying docs knowledge into `Documentation/.ai/`** (STARTED — `.ai/rules/writing-correct-examples.md` + `astro-starlight-site.md` exist). Add more skills/rules (tour-voice writing, Diátaxis page authoring, the comparison-page method, an "add a docs page" skill), and **wire the `.github/instructions/` + `.claude/rules/` symlinks** per `Chronicle/.ai/rules/managing-ai-rules.md` so both AI tools pick them up. Fold in awesome-docs takeaways.

---

**Verified state right now:** the site is **green — 678 pages · 0 lint errors · 0 broken links** (`cd Documentation/web && npm run check`). Everything committed on `docs-overhaul` across repos; **nothing pushed**. All working trees clean (only untracked `Screenshot*.png`, `.claude/scheduled_tasks.lock`, and `.github/skills/` remain — leave them).

**Session 2026-05-31 (product boundaries + differentiator + master plan — COMMITTED, NOT pushed; 671 → 674 pages, 0/0):** built the **product-isolation / combination / brownfield narrative** the user asked for — Chronicle and Arc are each usable standalone, with a one-way dependency (**Arc can sit on top of Chronicle; Chronicle never knows Arc**). Delivered: reworked site `why-cratis.mdx` ("Use them on their own — or together" + decision table + pluggable-persistence diagram + fixed "good fit" so Arc-fit ≠ event-sourcing-fit); new **`Arc/Documentation/arc-without-event-sourcing.md`** (canonical standalone slice over MongoDB — command writes a document directly, live model-bound query via change streams, identical React; the read side is identical with/without Chronicle, only `Handle()`'s write path differs); new site **`adopting-cratis.mdx`** (greenfield/brownfield, decision flowchart, wired into Overview topic); additive reframes of Arc `why-arc.md` + `index.mdx` (pluggable persistence: Chronicle/Mongo/EF as peers) and Chronicle `index.mdx` (directional close). Built **`Arc/Documentation/understanding-the-proxy-boundary.mdx`** — the differentiator taught as a concept (`FullStackTabs` rename demo; front door + why-cratis "full-stack type safety" link to it). Created **`Documentation/web/MASTER-PLAN.md`** — the living depth roadmap; it now holds two user-proposed **big bets**: a **"Cratis Stack" umbrella hero topic** and **AI-native development** (grounded in local `AI/` repo = `.ai/` agents/skills + `cratis init`, and `Chronicle.Mcp` = MCP server). **CORRECTION pending:** the existing `studio.mdx` mis-describes Studio as a runtime control room — per the user, **Studio is an independent Event Modeling / Spec-Driven Development canvas** (read `Studio/Source` + cratis.studio); Workbench + CLI are the inspect/interact-with-Chronicle tools. Verified architecture facts: see the `cratis-product-boundaries` memory. The real sidebar source is `src/generated/topics.json` (NOT the stale `sidebar.json`). **Also this session:** rebranded the first nav topic **Overview → "Cratis Stack"** with a new `cratis-stack` hero landing (tours design → build → operate, AI woven in); added an **`ai-native-development`** page (build-side `.ai` skills/agents + operate-side `cratis init` and the Chronicle MCP server); and **corrected `studio.mdx`** — Studio is an **independent Event Modeling / Event Storming canvas that generates type-safe C#**, NOT a runtime inspector (Workbench is a CLI command — `cli/Source/.../Workbench` — for inspecting a running store). `MASTER-PLAN.md` is the depth roadmap; its two big bets (Cratis Stack topic, AI-native) are now 🟡 shipped. Commits this session: Arc `36f625de`, Chronicle `d2100f15a`, Documentation through `c5fc7a1` (+ these meta updates) — all on `docs-overhaul`, **not pushed**. **Depth pass (same session):** added Arc `understanding-identity-and-access` + `understanding-the-proxy-boundary`; modernized `core/authorization.md` to `[Command]`/`Handle()`; added Chronicle `understanding-constraints-and-evolution`; enriched the Components `DataPage` reference and **fixed a tutorial bug** (`detailsComponent` is lowercase; `detailsTitle`/`initialSizes` aren't real props — verified vs `Components/Source/DataPage/DataPage.tsx`). All `Understand`-bucket explanation pages link down to existing reference (the "two voices" pattern). `MASTER-PLAN.md` reflects all of it. **Snippet-correctness audit** (delegated to a subagent, findings verified vs source): Arc tutorials + getting-started and Components `list-it`/`act-on-it` are **clean**; fixed Components recipes `displaying-data` + `list-screen-with-actions` (wrong `@cratis/components` import → `@cratis/components/DataTables`; missing required `emptyMessage`/`title`/children). **Round 2** (Chronicle hands-on + Arc scenarios + capstone/front-door + the new explanation pages): all clean **except** four more real bugs, now fixed — the Chronicle `read-model` tutorial used a **fabricated `static On(event)` projection** (→ real `[SetFrom]`/`[SetValue]`); `scenarios/test-a-slice` missed the `eventSourceId` arg on the out-of-process `ShouldHaveAppendedEvent`; and **`backend/mongodb/getting-started.md` had `UseCratisArc()` on the builder** (→ `AddCratisArc()`; `UseCratisArc` is app-only) — a bug that had propagated into the new `arc-without-event-sourcing` page. **8 snippet bugs fixed across 2 audit rounds.** Reference pages clearly still harbor such bugs — this audit is a high-value **recurring** activity (delegate to subagents, verify each finding vs source before fixing). **Round 3** (Arc backend reference setup pages) found **systemic doc-debt**: `backend/core/getting-started.md` teaches **non-existent `ICommand`/`ICommandHandler<T>`/`IQuery`/`IQueryHandler` interfaces** + `UseCratisArc("url")` + `CommandResult.Success`-as-property; `backend/asp-net-core/configuration.md` has `UseCratisArc()`-on-`IHostBuilder`. Fixed `reducers/getting-started.md` (retrieval → `_eventStore.ReadModels.GetInstanceById<T>`); the rest is captured as a **precise HIGH-PRIORITY backlog in `MASTER-PLAN.md`** (each with the verified fix) — deliberately left for a focused pass rather than rushed at high context. **9 snippet bugs fixed so far; a documented batch remains.** **Then (same session):** did the **Arc reference cleanup** — `backend/core/getting-started.md` + `backend/asp-net-core/configuration.md` rewritten to real APIs (`ArcApplication.CreateBuilder`, `app.UseCratisArc()` no-args with URL from `ArcOptions.Hosting.ApplicationUrl`, model-bound `[Command]`/`Handle()` + `[ReadModel]` queries, `AddCratisArc` on the host builder); `authorization.md` **fully fixed** — the deep sections' blocker was resolved by finding the real current-user accessor (**inject `IHttpContextAccessor`, read `HttpContext.User`** — verified in Studio `OrganizationSetup.Handle`); they now use that + `ValidationResult.Error` guards + `CommandScenario<T>` tests. (New follow-up surfaced: `backend/testing/command-scenario.md` cites a non-existent `StubIdentityProvider` — logged in MASTER-PLAN.) And **wired the awesome-docs QA stack** — markdownlint + lychee now join Vale as **graceful, advisory** steps in `npm run check` (skip when the tool is absent — no deps/lockfile touched; **install them in CI / locally to activate**, they currently skip everywhere). Expressive-Code power-feature plugins remain (build-import risk). **~11 snippet/API bugs fixed total this session.**

**Per-repo HEADs at handover** (start from these):
- **Documentation** `5be0af6` "Bucket the Components tutorial and PrimeReact bridge in the sidebar nav" (+ this session's handover-update commit on top — run `git log`)
- **Chronicle** `d9e459ad2` "Reshape host get-started variants as guided tours"
- **Arc** `3428265b` "Add Arc full-stack tutorial and scenario recipe catalog" (was `bdbaede3`)
- **Fundamentals** `c59c939` "Rewrite Fundamentals C# landing with a concept walkthrough"
- **cli** `ccb3ee4` "Rewrite CLI getting-started as a guided tour"
- **Components** `8aa42a8` "Add Components tutorial and a Coming from PrimeReact bridge" (was `27ef6f6`)

**What landed across this overhaul (don't redo):** front door rebuilt around the cratis.io "Cratis stack" model; **Studio** "coming soon" vision page (`web/src/content/docs/studio.mdx`) wired into the Overview topic; 5 reusable components in `web/src/components/` importable via `@components` (`FullStackTabs` = synced C#/TS tabs, `OsAwareTabs`, `YouWillLearn`, `Recap`, `StackDiagram`); a cross-product **glossary** (`web/src/content/docs/glossary.md`); a strengthened **lint-docs.mjs** (Google/MS style nudges — weasel words + heading-punct — as non-failing warnings, ~159 of them, advisory only); an **optional Vale** prose layer (`web/.vale.ini` + `web/.vale/styles/Vocab/Cratis/` + `web/scripts/lint-prose.mjs`, wired into `npm run check`, **skips gracefully when `vale` isn't installed**); tour-voice reshapes of flat pages (Chronicle projections + read-models; Arc commands, queries, frontend, backend, and the react/core/mvvm section landings; Fundamentals C#); a **cli Scenarios** section (fix-a-stuck-observer, replay-a-projection, verify-events); a Components **"choosing a component"** guide; and Mermaid **diagrams on concept pages** (event-source, event-sequence, observers). **Session 2026-05-31 (cont.):** the four flat getting-started pages rewritten as guided-tour `.mdx` (CLI, Components, Arc backend `your-first-command`, Arc frontend) — Steps/Tabs/Aside + a diagram each, all threading one `RegisterAuthor`/`AllAuthors` author slice; Mermaid **diagrams added to the remaining 7 core concept pages** (event-store, namespaces, event-type, event, constraints, projection, subject); and the **Chronicle host get-started variants** (console/worker/aspnetcore) reshaped into guided host-specific tours (scenario opening, host-placement diagram, recap, cross-links) with all code/includes preserved. The converter **keeps `.mdx`** (sync-content.mjs line 304) — author getting-started pages as `.mdx` and import `{ Steps, Tabs, TabItem, Aside }` from `@astrojs/starlight/components` (proven by Chronicle's `get-started/index.mdx`). Valid `<TabItem>` icons include `apple`, `linux`, `seti:c-sharp`.

**Session 2026-05-31 (Arc content-FORM build-out — COMMITTED, NOT pushed; 653 → 666 pages, 0/0):** gave Arc the Marten/Wolverine *content form* it was missing (OPEN #2). Two new product-level sections, both authored in the product repo and bucketed in `sync-content.mjs`:
- **Threaded tutorial** `Arc/Documentation/tutorial/` ("Build a library, full-stack") — `index.md` + 5 problem-driven `.mdx` chapters: `first-slice` (command→event→projection→query→React, with `FullStackTabs`), `validation` (`ConceptValidator` + uniqueness business rule via injected read model returning `Result<ValidationResult,…>`), `books-and-relationships` (`[ChildrenFrom<…>]` projection — mirrors Studio's `ProjectHierarchy`), `real-time` (observable queries as subscriptions + a reactor), `authorization` (`[Roles(...)]` + conclusion recap). Threads the same `RegisterAuthor`/`AllAuthors` author/library slice as getting-started, but goes full-stack + multi-feature. Arc commit `3428265b`.
- **Scenario/recipe catalog** `Arc/Documentation/scenarios/` — `index.md` + 6 recipes (validate-a-command, return-a-result-or-error, react-to-an-event, query-related-data, run-a-command-from-react, test-a-command via `CommandScenario<T>`), cross-linking the existing `backend/authorizing-commands-and-queries.md`. Same commit.
- **Nav:** Arc `toc.yml` gains Tutorial + Scenarios; `sync-content.mjs` Arc buckets now `Get started`(Tutorial) / `Understand` / `Guides`(Scenarios). Documentation commit `dff8cca`. The "Tutorial" sidebar badge applies automatically (SIDEBAR_BADGES rule matches label `Tutorial`).
- **All C# verified against Studio source** (`/Volumes/sourcecode/repos/cratis/Studio/Source`): `CommandValidator`/`ConceptValidator`, `Result<ValidationResult,…>` + read-model injection (`SetupOrganization`), `[ChildrenFrom<…>]` (`ProjectHierarchy`), `IReactor`+`ICommandPipeline`, `[Roles(...)]`, `CommandScenario<T>`. **Gotcha confirmed:** intra-doc links to a `.mdx` page must be **extension-less** (`./validation`, not `./validation.mdx`) — the converter's slugify strips the dot, turning `.mdx` into `mdx` and breaking the link. **Still OPEN under #2:** Components has no threaded tutorial / "coming from plain PrimeReact" bridge yet (Arc's bridge `coming-from-mediatr-and-mvc.md` already existed and is strong).

**Session 2026-05-31 (Components content-FORM build-out — COMMITTED, NOT pushed; 666 → 671 pages, 0/0):** closed the Components half of OPEN #2. (1) **Threaded tutorial** `Components/Documentation/tutorial/` ("Build the library screen") — `index.md` + 3 `.mdx` chapters: `list-it` (`DataTableForObservableQuery` live table in a `Page`), `act-on-it` (`useDialog`+`CommandDialog` add/rename/remove, the live-update payoff), `list-and-detail` (`DataPage` with `.MenuItems`/`.Columns` + `DetailsComponent` showing the selected author's `books` + conclusion recap). Builds the UI for the Arc tutorial's backend — cross-product thread (consumes `AllAuthors`/`AllAuthorsWithBooks`). (2) **Migration bridge** `Components/Documentation/coming-from-primereact.md` — Wolverine-from-mediatr style, side-by-side raw-PrimeReact-vs-Components per concept (form, table, list-detail) + map table + what-stays/what-changes; distinct from `why-components.md`'s adoption framing. (3) **Fixed an orphan:** `choosing-a-component.md` existed + was bucket-referenced but missing from the root `toc.yml` — added it. Nav: Components buckets restructured to `Get started`(Getting started/Tutorial/Choosing a component/Styling) + new `Understand`(Why Components/Coming from PrimeReact) + Recipes/Components/Reference. All component APIs verified against **Studio** `.tsx` (`Users.tsx`, `Authentication.tsx`) + the Components reference (`DataPage/details-panel.md`). Commits: Components `8aa42a8`, Documentation `5be0af6` (bucketing) + handover. **With this, OPEN #2 (content FORM) is DONE for all three hero products** (Chronicle, Arc, Components) — see §8.

**Corrected facts (earlier handover/notes had these WRONG):**
- The Chronicle **tutorial DOES have chapters** — `Chronicle/Documentation/tutorial/{index,first-event,read-model,reacting}.md`. It is the real voice exemplar, NOT empty. (It's 3 chapters + index, not "4 chapters".)
- **`check-links.mjs` already validates relative links** (it resolves `./foo`/`../foo` against the page). Do not "fix" it; there is no dead-relative-link backlog.
- **Platform is settled: Astro Starlight stays** (free/OSS), hosted on **GitHub Pages** via `Documentation/.github/workflows/docs-site.yml` (needs Pages enabled + a `DOCS_CHECKOUT_TOKEN` secret — see §9). There is **no Mintlify** anywhere; ignore any mention of it.

**⚠ CRITICAL LESSON — the tool channel is intermittently unreliable (RTK output truncation / stale reads):**
1. **Reads sometimes return STALE or FABRICATED file content.** This caused real damage earlier: I invented a "missing tutorial chapters" + "dead relative links" problem that didn't exist, and overwrote two correct pages (`Chronicle/.../scenarios/index.md`, `Arc/.../backend/chronicle/index.md`) with broken links — committed, then **reverted with `git revert`**. **Mitigation: fresh-`Read` a file immediately before every `Edit`; verify claims with an independent `grep`/`git show`; run `npm run check` after every change; never trust a remembered read.**
2. `git reset --hard` is **blocked by the harness** — use **`git revert`** to undo a commit.
3. `Edit` **fails safe**: when read content is contaminated, its string-match refuses to apply (no write). Lean on that — a failed Edit is the signal your read was stale; re-Read and retry.

**Best next work (high-confidence, verified-needed):**
- **DONE — don't redo** (last 3 sessions): the getting-started tour pass (CLI, Components, Arc backend+frontend) + 7 concept-page diagrams + Chronicle host variants; the **Arc content-FORM** build-out (threaded tutorial `Arc/Documentation/tutorial/` + scenario catalog `Arc/Documentation/scenarios/`); and the **Components content-FORM** build-out (tutorial `Components/Documentation/tutorial/` + `coming-from-primereact.md` bridge). **OPEN #2 (mirror Marten/Wolverine content FORM) is now closed for all three hero products.** The tutorials all thread one `RegisterAuthor`/`AllAuthors`/`AllAuthorsWithBooks` author-and-books library app, Arc backend → Components UI — keep that thread if you extend them.
- **Next, pick one:**
  - **#4 depth** (the main remaining frontier): fill out reference + guides for a specific feature area of a hero product, for novices *and* experts. Highest-value, open-ended, continuous.
  - **"Understanding…" explanation pages** (the last sliver of #2): 1–2 standalone explanation pages where an area still reads like flat reference — e.g. Arc "Understanding the full-stack/proxy boundary". Optional.
  - **#0 foundation tooling** (still pending, needs `package.json`/dep additions — flag first): Expressive-Code power features (collapsible regions, line numbers, expected-output `data-disable-copy`) and the QA stack (Vale/markdownlint/lychee); wire to degrade gracefully where binaries aren't installed.
- **Why pages** (`Arc/why-arc.md`, `Components/why-components.md`, `Chronicle/why-event-sourcing.md`) were assessed already strong and intentionally left — light-touch only.
- **Do NOT** mass-remove the ~179 weasel-word warnings — the conversational "just/simply" usage is the deliberate tour voice; the linter is a signal, not a mandate.

---

## 1. The goal

Make the Cratis documentation **excellent and launch-ready** for a worldwide .NET podcast (≈ mid-June 2026) that will drive a spike of evaluators. The bar: an **Aspire-class** experience — a newcomer goes *why → running → building real things → reference* on a guided path; novices and experts both have an on-ramp; the docs feel like a **guided tour** (Marten/Wolverine/Aspire style), not a reference dump.

Scope is the hero stack — **Chronicle** (event sourcing), **Arc** (full-stack CQRS framework), **Components** (React) — plus **CLI**, **Fundamentals**, and **Contributing**. The differentiator we lead with everywhere: Cratis is **full-stack and type-safe** (C# → generated TS → React); the alternatives stop at the backend.

Full approved plan: `/Users/sindrewilting/.claude/plans/zesty-pondering-fog.md`.

## 2. Current status

A modern **Astro Starlight** site lives in `Documentation/web/`. It aggregates all product docs and builds clean:
**~647 pages · 0 lint errors · 0 broken internal links** (verified by two custom gates).

All six products are migrated and on a consistent, curated **Diátaxis** navigation. ~45 pages were hand-written/rewritten (front door, why-pages, threaded tutorial, scenarios, recipes, "coming from X" bridges incl. Marten/Wolverine/Kurrent comparisons, glossary, architecture, deep dives, troubleshooting, CLI landing). Everything is committed on `docs-overhaul` branches; **nothing is pushed**.

**A large aspire.dev-style visual overhaul landed (the user's strongest recent push — "copy most of Aspire"):** the converter keeps `.mdx`; brand fonts (Inter + JetBrains Mono); a **product icon rail** (`starlight-sidebar-topics`) — Chronicle/Arc/Components/CLI/Fundamentals/Contributing + an Overview topic, each switchable with a boxed icon; a per-page **Copy-markdown / Open-in-AI / Share** row (`starlight-page-actions`); scroll-to-top, image-zoom, laserwave/slack-ochin code themes; a roomier clamped ToC; a crafted sidebar with **Quickstart/Tutorial badges** and a larger font; a rebuilt splash **front door**; and a rich **per-topic Overview landing for every product** (+ the cross-product `why-cratis`), built from new `TopicHero.astro` + `SimpleCard.astro` via a `@components` alias. Brand stayed **Cratis blue + the Cratis logo** (Aspire *structure*, not its purple). Gate still 647 / 0 / 0.

## 3. Run & verify locally

**Prerequisites**
- Node 20+.
- The product repos cloned as **siblings** of this repo, all on the `docs-overhaul` branch:
  `<parent>/{Documentation, Chronicle, Arc, Components, Fundamentals, cli, .github}`.
  (`Arc` = the `Cratis/ApplicationModel` repo cloned as `Arc`; `.github` = `Cratis/.github`.)

```bash
cd Documentation/web
npm install
npm run dev          # http://localhost:4321  (sync + dev server)
npm run check        # build + lint:docs + check:links  — the full gate
```
`npm run check` must end with `Complete!`, `0 error(s), 0 warning(s)`, and `0 broken`. Verification checklist is in `Documentation/web/README.md`.

## 4. Repos & git state

Work spans these repos, each on branch **`docs-overhaul`**, all **committed, none pushed**:
`Documentation` (the site), `Chronicle`, `Arc`, `Components`, `Fundamentals`, `cli`, `.github`.
(`Samples` and `Templates` are branched but unchanged so far.)

To push / open PRs when ready, use the **ship-changes** skill. Per project preference, **do not push/commit without explicit approval** — the user has been approving commits explicitly.

## 5. How the site is built (architecture)

**Content source of truth = each product repo's `Documentation/` folder.** The site does not own the content; it converts it at build time.

- `web/scripts/sync-content.mjs` reads each product's `Documentation/` (resolved as a **sibling** clone first, falling back to a git submodule) and converts DocFX-style Markdown into Starlight content under `web/src/content/docs/<product>/`. **Those generated dirs are git-ignored — never edit them by hand.** Edit the source in the product repo and re-sync (`npm run sync`).
- The conversion handles: frontmatter (parses YAML, keeps `title`/`description`/`sidebar`, drops DocFX keys like `uid`), DocFX alerts (`> [!NOTE]` → `:::note`), `<xref:>` and `[text](xref:UID)`, `[!INCLUDE]` (inlined), `.md`/`toc.yml` link stripping, and **slug normalization** of link paths.
- **Sidebar** is generated from each product's `toc.yml` into `web/src/generated/sidebar.json` (git-ignored), imported by `astro.config.mjs`. Chronicle/Arc/Components are grouped into **Diátaxis buckets** via per-product `buckets` config in `sync-content.mjs` (`applyBuckets`). Components/Chronicle/Arc use `sidebarMode: 'toc'`; the converter validates every sidebar slug against actually-built pages and drops broken toc entries.
- **Site-level pages** (not owned by any product) are authored directly in `web/src/content/docs/` as `.mdx`: `index.mdx` (front door), `why-cratis.md`, `build-a-full-app.mdx` (capstone walkthrough), `samples.mdx`, `api-reference.md`, `404.md`, and `comparisons/` (Marten/Wolverine/Kurrent). Top-level nav for these is prepended in `astro.config.mjs` (`sidebar.unshift(...)`).
- **Quality gates** (both wired into `web/.github`-style CI workflow `Documentation/.github/workflows/docs-site.yml`, and as npm scripts):
  - `npm run lint:docs` (`scripts/lint-docs.mjs`) — fails on non-descriptive link text (`[here]`, `[see documentation]`) and leftover DocFX-isms (`<xref:`, `[!INCLUDE]`, unconverted alerts).
  - `npm run check:links` (`scripts/check-links.mjs`) — verifies every internal link resolves to a built page (Starlight doesn't). Hard gate; currently 0 broken.
- Plugins: `starlight-sidebar-topics` (the product icon rail; topics come from `src/generated/topics.json`, emitted by `sync-content.mjs` — one topic per product + a static "Overview" topic in `astro.config.mjs`; product pages are mapped to topics by leading-slash glob in the plugin's `topics` option), `starlight-page-actions`, `starlight-scroll-to-top`, `starlight-image-zoom`, `starlight-llms-txt`, `astro-mermaid`, Pagefind. Fonts: Inter + JetBrains Mono via `@fontsource-variable/*` in `customCss`, pointed at by `--sl-font`/`--sl-font-mono`. Shared site components live in `web/src/components/` (`TopicHero.astro`, `SimpleCard.astro`) and are importable from product `.mdx` via the **`@components/*` alias** (in `tsconfig.json`). Branding: `web/src/assets/cratis-mark-{light,dark}.svg` + all accent/ToC/sidebar/topic-rail polish in `web/src/styles/cratis.css`.
- **Per-topic Overview landings**: every product's `index.mdx` (in its product repo) and the site-level `why-cratis.mdx` are rich landings (`<TopicHero>` + `<SimpleCard>` grids). Caveat: `SimpleCard`/`TopicHero` link props are **JSX attributes, so `check-links.mjs` does NOT validate them** (it only scans markdown `](…)`). Verify card `link="…"` targets by hand (grep `dist/<slug>/index.html`) when editing these.

## 6. What's done

- **Platform**: Starlight site, real Cratis logo (theme-adaptive), Pagefind search, `llms.txt`, Mermaid, ToC capped to H2 + compacted, refined sidebar category labels.
- **All 6 products migrated** with curated, bucketed Diátaxis nav.
- **Onboarding**: front door (C#↔TS tabs, product cards incl. CLI, comparison table), getting-started for each product, a **guided-tour Chronicle tutorial** (4 chapters), cross-product capstone walkthrough, Samples gallery.
- **Explanation**: Why Cratis/Arc/Components, glossary, architecture (diagrams), observer/reactor/reducer guide, modeling-events, designing-read-models, consistency deep dive, vertical-slices ("why backend+frontend together"), when-to-use.
- **How-to**: Chronicle scenarios (6), Components recipes (4), Arc authorization recipe, Chronicle + Arc troubleshooting.
- **Comparisons & migration**: "How Cratis compares" + per-tool pages for **Marten, Wolverine, Kurrent/EventStoreDB** (verified against the local Marten/Wolverine checkouts + EventStoreDB client API), plus CRUD/EF and MediatR/MVC bridges.
- **Diagrams** on the front door, capstone, architecture, tutorial, and every core concept landing.
- **The `.ai/rules/documentation.md` rule was rewritten** to encode the new standards (Diátaxis, onboarding, link-text, snippets-from-source, llms.txt).

## 7. The writing bar — IMPORTANT

The user's strongest qualitative feedback: **the docs must take the reader on a tour, like a teacher** — Marten/Wolverine/Aspire style — not state facts like a reference. The tour-style checklist is saved in memory (`feedback_docs_content_patterns.md`). In short:
1. Open with a concrete scenario, not a definition. 2. Name the friction, then the relief. 3. "Let's…" with chronological verbs. 4. After code, **explain the invisible**. 5. Recap before pivoting. 6. Anticipate doubt with asides. 7. **Show the result**. 8. Steps/Tabs/asides/diagrams; organize by workflow. 9. Forward link at the end of every section.

The **Chronicle tutorial** (`Chronicle/Documentation/tutorial/*`) is the reference example of this voice — match it.

**Inspiration is cloned locally — study these for CONTENT, structure, and voice; don't guess or invent:**
- `~/src/repos/aspire.dev/src/frontend/src/content/docs/` — the gold standard for *visual/structural* polish. `.mdx` with `<Steps>`, `<Aside>`, `<Code title=... />`, `OsAwareTabs`, `LearnMore`, expected-output blocks (`data-disable-copy`), architecture-diagram-first. Read `get-started/install-cli.mdx` and `get-started/what-is-aspire.mdx`.
- `~/src/repos/marten/docs` and `~/src/repos/wolverine/docs` — the gold standard for *content & teaching*. The user specifically wants our docs to feel like these: they **take you on a tour / walk you through**. Mine their patterns and reuse them as a template (don't copy text):
  - **Threaded narrative tutorials** that build one system across chapters (Marten's `docs/tutorials/` "Freight & Delivery"), each chapter opening with learning goals and ending with a recap + handoff.
  - **Scenario / recipe catalogs** (Marten `docs/scenarios/`) — task-oriented "how do I do X".
  - **"Coming from X" bridge guides** (Wolverine "For MediatR Users", Marten "Postgres for SQL Server users").
  - **"Understanding …" explanation pages** separate from reference (Marten `docs/events/learning.md`).
  - Prose that names the friction, narrates what the code does, and explains the *why* inline.

## 8. What the user asked us to fix / work on — ledger

This is the running record of the user's **explicit** feedback and priorities across the project. Treat the OPEN items as the active backlog; honor the DONE items (don't regress them).

### Already addressed (✓ — keep honoring, don't redo)
- **Tour-style threaded Chronicle tutorial** written as the voice exemplar for the whole site.
- **"On this page" ToC was *way* too big** → capped to H2 (`maxHeadingLevel: 2`) and compacted (`cratis.css`).
- **Left sidebar category labels looked "cheap"** → refined to small/uppercase/tracked/muted (`cratis.css`).
- **CLI front and center** — added to the front door, plus a tour-style CLI landing + getting-started covering *why / how / install*. (Deepen the tour voice further — see OPEN #1.)
- **Comparisons + migration guides** for **Marten / Wolverine / Kurrent** (+ CRUD/EF and MediatR/MVC bridges), including honest "why not them / where we differ".
- **Why co-locate backend + frontend** — the `vertical-slices` explanation page answers this directly.
- **Real Cratis logo / icons** (theme-adaptive marks) — replaced the placeholder/wrong imagery.
- **`.github` / Contributing wired in** as a first-class product in the nav.
- **Work committed across all `docs-overhaul` branches** to save progress; ready to push via **ship-changes** once approved.
- **Quality gates green** — 0 broken links, 0 lint errors, descriptive link text enforced.

### Done in the visual-overhaul session (✓ — don't redo)
- **`.mdx` kept through the converter**; **brand fonts** (Inter + JetBrains Mono); **product icon rail** (`starlight-sidebar-topics`); **page-actions / scroll-to-top / image-zoom / code themes**; **roomier ToC + crafted sidebar with Quickstart/Tutorial badges + bigger font**; **rebuilt splash front door**; **per-topic Overview landings for all products + the cross-product `why-cratis`** (TopicHero + SimpleCard). Chronicle **getting-started** rewritten as an `.mdx` tour (the new getting-started exemplar, alongside the tutorial).

### Done in the craft session (2026-05-31) (✓ — don't redo)
- **Front door rebuilt around the cratis.io "Cratis Stack"** (`web/src/content/docs/index.mdx`): "Build better software, faster." hero, 3-step `dotnet new` quickstart, `<StackDiagram>`, synced C#/TS slice via `<FullStackTabs>`, CQRS mermaid, comparison table, CTA grid.
- **Cratis Studio added** as a lean "coming soon" **vision page** (`web/src/content/docs/studio.mdx`) — wired into the Overview topic nav (badge "Soon") and the stack; not a full product topic (its real docs are deployment/dev stubs).
- **New reusable components** in `web/src/components/` (import via `@components`): `FullStackTabs` (synced C#/TS tabs — the differentiator), `OsAwareTabs` (OS-detecting install tabs), `YouWillLearn` + `Recap` (tutorial pedagogy callouts), `StackDiagram`.
- **Tour-voice reshape** of the worst flat pages (edited in product-repo source, re-synced): Arc `frontend/index.md`→`.mdx` (full-stack tabs, mermaid; **API verified** = proxy static `.use()`, not named hooks), Chronicle `projections/index.md` (why + consistency decision table), Arc backend `commands/index.md` + `queries/index.md` (scenario-first + decision tables), Fundamentals `csharp/index.md` (define-your-first-concept walkthrough).
- Gate re-verified **green: 648 pages / 0 lint / 0 broken** after every change.

### Craft session — ALL COMMITTED & VERIFIED green (2026-05-31, 653 pages / 0 errors / 0 broken)
Committed on `docs-overhaul` (NOT pushed). Platform decision: **Astro Starlight stays** (free/OSS, GitHub Pages). NO Mintlify.
- **Front door** rebuilt around the cratis.io "Cratis stack" + **Studio** "coming soon" vision page + **Glossary** (one-term-one-definition, all links verified) + 5 reusable components (`FullStackTabs`/`OsAwareTabs`/`YouWillLearn`/`Recap`/`StackDiagram`). [Documentation repo]
- **Prose linter strengthened** (`web/scripts/lint-docs.mjs`): error-level authoring markers (TODO/FIXME/TBD/lorem) + warning-level Google/MS style nudges (weasel words, heading end-punctuation). Warnings DON'T fail the gate; zero new deps. Currently 156 warnings across existing pages — a ready-made cleanup backlog.
- **Tour-voice reshapes** (product repos): Chronicle projections + read-models; Arc commands + queries + frontend + backend; Fundamentals C# (concept walkthrough).
- **New how-to content**: cli **Scenarios** section (fix-a-stuck-observer, replay-a-projection, verify-events-were-appended) + Components **choosing-a-component** decision guide.

### Getting-started tour pass + concept diagrams + host variants — COMMITTED & VERIFIED green (2026-05-31 cont., 653 / 0 / 0, NOT pushed)
- **Getting-started rewritten as guided-tour `.mdx`** (one commit per repo): CLI (`cli` ccb3ee4), Components (`Components` 27ef6f6), Arc backend `your-first-command` + Arc frontend (`Arc` bdbaede3). Each: scenario opening, `<Steps>`, an `<Aside>`, a Mermaid diagram, recap, forward links — all threading **one** `RegisterAuthor`/`AllAuthors` author slice so backend→frontend→Components read as one story. CLI adds OS-aware install `<Tabs>` + a connection-resolution diagram.
- **7 remaining concept-page diagrams** (`Chronicle` e3b814f44): event-store, namespaces, event-type, event, constraints, projection, subject — additive Mermaid only, fits each page's content.
- **Chronicle host variants reshaped** (`Chronicle` d9e459ad2): console/worker/aspnetcore now guided host-specific tours — scenario opening that contrasts the host, host-placement diagram, recap, cross-links between the three; frontmatter title+description added; all code/snippet-links/includes (prereq, docker, common, mongodb) preserved verbatim.
- **Mechanism confirmed**: the converter KEEPS `.mdx` (sync-content.mjs:304) — supersedes the stale OPEN #3 below. Author product getting-started as `.mdx`, import Starlight components from `@astrojs/starlight/components`. Renaming `.md`→`.mdx` keeps the slug (inbound links are extension-independent), but **update the page's `toc.yml` href** to `.mdx`.

### Open — work on next (▶ — active backlog, priority order)
0. **Foundation tooling still pending** (user authorized "foundation first" but content was prioritized): Expressive-Code power features (collapsible regions, line numbers, expected-output `data-disable-copy`) and the QA stack (Vale + Microsoft style + house Vocab, markdownlint, lychee) wired into `npm run check` — both need package.json/dep additions; lychee/Vale binaries aren't installed in this env so wire them to degrade gracefully.
1. **Tour-voice pass** — ✅ getting-started (Arc backend+frontend, Components, CLI), ✅ Chronicle host variants (console/aspnetcore/worker), and ✅ Why pages (assessed already strong, intentionally left) are all DONE (see the committed block above). Remaining under this bar: any pages still reading like flat reference that aren't getting-started — keep matching the Chronicle tutorial + getting-started voice (Marten/Wolverine/Aspire, §7). Optional: build Aspire's `QuickStartJourney` vertical-timeline component for the Overview landings (the user liked it).
2. ✅ **Mirror Marten/Wolverine content FORM, not just style** — DONE for all three hero products (the user flagged this *twice*). Each now has a **threaded tutorial**, a **scenario/recipe catalog**, and a **"coming from X" bridge**: ✅ **Chronicle** (tutorial + scenarios + CRUD/EF bridge), ✅ **Arc** (tutorial + scenario catalog + `coming-from-mediatr-and-mvc.md`), ✅ **Components** (tutorial + recipes + chooser + `coming-from-primereact.md`). **Remaining sliver under #2:** dedicated **"Understanding …" explanation pages separate from reference** — Chronicle has concept pages, but Arc/Components could use one or two standalone "Understanding the full-stack flow / the proxy boundary" explanations if any area still reads as flat reference. Otherwise this item is effectively closed; the next frontier is **#4 (depth)**.
3. ~~**Enable `.mdx` in product pages**~~ ✅ RESOLVED — the converter already keeps `.mdx` (sync-content.mjs:304); the getting-started pages now use `<Steps>`/`<Tabs>`/`<Aside>`. Pattern for any future product `.mdx`: add frontmatter `title`+`description`, no body H1, import from `@astrojs/starlight/components`, update the `toc.yml` href to `.mdx`.
4. **Go deeper — cover all aspects & features, for novices AND experts.** The Diátaxis structure and the dual on-ramps (a "new to event sourcing" track and an expert "fast path / coming-from-X" bridge) are in place; **depth is continuous work** — fill out reference and guides for every major feature of each hero product (see the plan's depth target).
5. **Keep the run-it-locally feedback loop tight.** The user runs `npm run dev` and gives concrete visual/UX feedback (ToC size, sidebar feel, imagery) — that's the highest-value signal. After visual changes, re-check that ToC/sidebar/spacing/diagrams/logo render cleanly in light + dark.
6. Optionally adopt an Aspire-style `OsAwareTabs`/`LearnMore`, or use Starlight's built-in `<Tabs>`/`<LinkCard>`/`<Steps>`.

Build-env-only items (API gen, Storybook embed, runnable capstone, deploy) are in §9.

## 9. Remaining work that needs YOUR environment (can't be done headless)

Tracked in `web/KNOWN-ISSUES.md`:
- **API reference generation** — .NET (DocFX over ~7 assemblies) + TS (TypeDoc over the `@cratis/*` packages). Needs the assemblies/packages built; verified twice it can't run without deps. An `api-reference.md` orientation page exists and links the packages.
- **Storybook embed** for Components — needs a deployed Storybook URL to iframe.
- **Runnable capstone sample app** — the `build-a-full-app` walkthrough is written; a verified building sample (Ada/Studio-grade) is a follow-up. Use **Studio** (`/Volumes/sourcecode/repos/cratis/Studio`) and **Ada** (`/Volumes/sourcecode/repos/hive/Ada`) as the quality reference — NOT `Samples/Library` (the user said it isn't good).
- **Deploy / cutover** — `docs-site.yml` workflow is ready; needs GitHub Pages enabled + a `DOCS_CHECKOUT_TOKEN` secret. The old DocFX site (`Documentation/Source/`) stays live until cutover. If serving under `cratis.io/docs`, set `base: '/docs'` in `astro.config.mjs`.

## 10. Gotchas (hard-won — read before editing)

- **Link rules differ by page type.** Product-repo pages go through the converter (it strips `.md`/`toc.yml`), so `[x](./foo.md)` is fine there. **Site-level `.mdx` pages do NOT** — write clean URLs (`/comparisons/marten/`, `./marten`), never `.md`. Run `npm run check:links` after any link change.
- **Astro slug rules** (replicated in `slugify`): lowercase, **strip dots** (`react.mvvm` → `reactmvvm`), keep `-`/`_`. Cross-product links use root-relative `/chronicle/...`.
- **Sections whose landing is `overview.md` (no `index.md`)** 404 on the bare URL (e.g. `arc/backend/tenancy/`) — link to the specific page.
- **The CLI is an operate/explore tool**, not a scaffolder. Don't invent `cratis new`/`run`. Real commands: `cratis get-started`, contexts, event/observer/read-model inspection, `cratis init` (AI tooling). Scaffolding is `dotnet new` templates (`Templates` repo).
- **Code examples must match real APIs** — verified ones: `[Command]`+`Handle()` on the record, `[EventType]`, `[ReadModel]`+`[FromEvent<T>]`, `collection.Observe()` returning `ISubject<>`, `[Roles(...)]`. Verify against **Studio** source (`/Volumes/sourcecode/repos/cratis/Studio/Source`) before writing C# snippets (the Ada checkout has moved).
- **Never edit `web/src/content/docs/{chronicle,arc,components,cli,fundamentals,contributing}/`** — generated. Edit the product repo source.
- **`web/src/content.config.ts` must use Starlight's `docsLoader()`/`docsSchema()`** — the committed version already does. (If you ever see `[glob-loader] The slug "index" is reserved`, it means the loader got swapped for a plain `glob()`; restore `docsLoader()`.)
- **Don't push or open PRs without explicit approval.**

## 11. Key references

- Plan: `/Users/sindrewilting/.claude/plans/zesty-pondering-fog.md`
- Memory (loaded each session): `docs-overhaul-project`, `feedback_docs_content_patterns` (tour checklist), `cratis-good-app-references` (Studio/Ada), `feedback_no_auto_commit`.
- This file: `Documentation/web/HANDOVER.md`. Site guide: `Documentation/web/README.md`. Known issues: `Documentation/web/KNOWN-ISSUES.md`.
- Inspiration: `~/src/repos/aspire.dev`, `~/src/repos/marten`, `~/src/repos/wolverine`.
- The authoring rule: `.ai/rules/documentation.md` (in each product repo via symlink; canonical in `.ai/`).
