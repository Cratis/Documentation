---
title: Start using Cratis AI
description: Configure selected Cratis rules and skills for one or more AI coding harnesses.
---

import { Aside, Steps } from '@astrojs/starlight/components';

Cratis AI has a canonical corpus of reusable **rules** and **skills**. Rules are
always-on repository guidance; skills are focused task workflows. Your repository
selects the Cratis profiles, languages, and harnesses it needs.

## Complete repository setup

Install the Cratis CLI, then run this from the repository root:

<Steps>

1. Run `cratis ai install`.
2. Select one or more harnesses, profiles, and languages when prompted.
3. Commit `.cratis/ai.json`, review the generated `.ai` content, and run your
   normal repository checks.

</Steps>

For automation, provide the selections explicitly:

```bash
cratis ai install \
  --harnesses claude,codex,pi \
  --profiles cratis/chronicle,cratis/arc \
  --languages csharp,typescript
```

The command reads the AI repository's `.ai/manifest.json` first, so the
interactive choices always reflect the harnesses, profiles, and languages that
the corpus currently offers. It then records the selection in `.cratis/ai.json`,
resolves the matching corpus, installs only Cratis-managed files under `.ai`, and
configures each selected harness. Pi receives only the resolved skill paths; it
does not decide which profiles or languages apply.

<Aside type="note" title="Your files remain yours">
Every installed text file carries a `cratis-ai-managed` marker and is recorded
with its installed hash. Add your own rules and skills anywhere under `.ai`.
Updates and uninstall never remove unknown files, and report a locally modified
managed file instead of overwriting it.
</Aside>

## Maintain the installation

```bash
cratis ai status
cratis ai update
cratis ai uninstall
```

`status` shows selections, installed corpus information, and local conflicts.
`update` adds, changes, moves, and removes only unchanged Cratis-owned content;
review conflicts before choosing how to resolve them. `uninstall` removes only
unchanged managed content and Cratis-created integrations.

## Plugin installation is complementary

A marketplace or native plugin is a convenient way to discover supported Cratis
skills in one harness. It is not the complete repository integration: it does
not compose repository rules, select profiles and languages for a project, or
manage multi-harness updates. Use `cratis ai install` for that complete setup.

## Contribute and verify skills

Skills may include executable verification scenarios. The verification runner
discovers them, applies deterministic assertions, and emits a pass/fail report
locally and in CI. Verification answers whether a skill behaves as intended;
it is not an authorship or provenance gate.
